## 2024-05-18 - Visual Indicators for Required Form Fields
**Learning:** Adding a visible required indicator (like an asterisk) to input labels improves form completion rates and accessibility by clearly communicating expectations without relying solely on screen readers reading the `required` attribute.
**Action:** Always append an `aria-hidden="true"` asterisk or similar required marker to labels for mandatory inputs across the application.
